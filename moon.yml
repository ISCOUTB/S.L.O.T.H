project:
    name: etl-design
    description: root

type: application

language: bash

toolchain:
    default:

tasks:
    parsers:
        command: echo "[root] running parsers..."
        deps:
            - formula-parser:run
            - ddl-generator:run
            - sql-builder:run
            - excel-reader:run
        options:
            runFromWorkspaceRoot: true
            runDepsInParallel: false
            persistent: true
            cache: false
            outputStyle: stream
