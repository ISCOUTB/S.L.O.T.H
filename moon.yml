project:
    name: etl-design
    description: project root

toolchain:
    defaults:

tasks:
    parsers:
        command: "noop"
        deps:
            - formula-parser:run
            - ddl-generator:run
            - sql-builder:run
            - excel-reader:run
        local: true

    test:
        command: noop
        deps:
            - formula-parser:test
            - ddl-generator:test
            - sql-builder:test
            - excel-reader:test
        local: true
