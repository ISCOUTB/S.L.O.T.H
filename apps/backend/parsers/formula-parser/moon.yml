project:
    name: formula-parser
    description: formula-parser service

type: application

language: typescript

toolchain:
    node:

tasks:
    test:
        command: echo "No tests defined"

    run:
        command: node
        args:
            - --env-file=.env
            - src/server.js
        toolchain: node
        options:
            runFromWorkspaceRoot: false
            persistent: true
