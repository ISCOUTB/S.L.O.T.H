name: CI - API Tests
on:
  pull_request:
    branches:
      - main
      - staging
    paths:
      - "apps/backend/api/**"
      - "packages/messaging-utils/messaging-utils-py/**"
jobs:
  api-tests:
    uses: ./.github/workflows/reusable/ci-tests.yml
    with:
      service_name: "api"
