apiVersion: v1
kind: Service
metadata:
  name: etl-excel-reader-svc
  namespace: development
  labels:
    app: etl-excel-reader
    component: etl-excel-reader
    environment: development
spec:
  type: ClusterIP
  selector:
    app: etl-excel-reader
    component: etl-excel-reader
    environment: development
  ports:
    - name: http
      port: 8001
      targetPort: 8001
      protocol: TCP
