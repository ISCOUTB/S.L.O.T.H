# Configurable variables for deployment
# Uncomment and adjust according to your needs

# Number of managers (must be odd: 1, 3, 5)
# manager_count = 1

# Number of workers
# worker_count = 2

# Your public IP for SSH (replace with your real IP)
# allowed_ssh_cidr = "AAA.BBB.CCC.DDD/32"

# Instance type (t3.micro is free with free tier)
# instance_type = "t3.micro"
